/**
 * Compares pair of identifiers and returns the match result.
 * @param firstId - First selected id
 * @param secondId - Second selected id
 * @returns {boolean} True if cards match, otherwise false
 */
export function matchPairs(firstId, secondId){
    return firstId === secondId;
}



